using UnityEngine;
using System.Collections;

public class Player2Colour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.material.color = new Color (0, 0, 20);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
