using UnityEngine;
using System.Collections;

public class BallColour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.material.color = new Color (0, 20, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
