using UnityEngine;
using System.Collections;

public class Player1Colour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.material.color = new Color (20, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
